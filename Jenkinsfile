pipeline {
    agent any
    parameters {
        string(name:'NAME', defaultValue:'', description: 'Akash')
    }
    stages {
        stage('How u doin!!') {
            steps {
                echo "Hello, ${params.NAME}!!"
            }
        }
    }
}
